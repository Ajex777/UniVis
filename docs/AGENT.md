## 新功能实现规则

1. 尽可能用 class 包装功能，并使用对象进行功能 ctx 管理。

2. 工具函数需要单独存放，并且建议分类存放。新增工具函数前，先检查现有函数能否复用；不能复用时，优先考虑能否做优雅的兼容性改造。

3. 当某个设计未来可能会被替换时，应及时使用抽象类定义广泛可复用的行为，并在其基础上实现子类，避免代码耦合过重导致后续扩展困难。

4. 网络层要保持简单，但实现上不能和业务逻辑耦合。

5. 为每个重要函数写注释，说明输入、输出以及核心功能。

6. 单个文件不要超过 250 行；如果超过，需要优先考虑拆类、拆函数。实在不好拆时，必须在文件开头说明原因。
